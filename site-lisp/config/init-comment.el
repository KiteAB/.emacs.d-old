;;; This file is used for Comment Settings
(use-package newcomment
  :bind (("C-' c d" . comment-dwin)
         ("C-' c l" . comment-line)
         ("C-' c b" . comment-box)))

(provide 'init-comment)
